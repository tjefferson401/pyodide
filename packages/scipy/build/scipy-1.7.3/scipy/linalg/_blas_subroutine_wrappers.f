c     This file was generated by _generate_pyx.py.
c     Do not edit this file directly.

      subroutine caxpywrp(
     +    ret,
     +    n,
     +    ca,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
        external caxpy
        integer caxpy
        integer ret
        integer n
        complex ca
        complex cx(n)
        integer incx
        complex cy(n)
        integer incy
        ret = caxpy(
     +    n,
     +    ca,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
      end

      subroutine ccopywrp(
     +    ret,
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
        external ccopy
        integer ccopy
        integer ret
        integer n
        complex cx(n)
        integer incx
        complex cy(n)
        integer incy
        ret = ccopy(
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
      end

      subroutine cdotcwrp(
     +    ret,
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
        external wcdotc
        complex wcdotc
        complex ret
        integer n
        complex cx(n)
        integer incx
        complex cy(n)
        integer incy
        ret = wcdotc(
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
      end

      subroutine cdotuwrp(
     +    ret,
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
        external wcdotu
        complex wcdotu
        complex ret
        integer n
        complex cx(n)
        integer incx
        complex cy(n)
        integer incy
        ret = wcdotu(
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
      end

      subroutine cgbmvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external cgbmv
        integer cgbmv
        integer ret
        character trans
        integer m
        integer n
        integer kl
        integer ku
        complex alpha
        complex a
        integer lda
        complex x(n)
        integer incx
        complex beta
        complex y(n)
        integer incy
        ret = cgbmv(
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine cgemmwrp(
     +    ret,
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external cgemm
        integer cgemm
        integer ret
        character transa
        character transb
        integer m
        integer n
        integer k
        complex alpha
        complex a
        integer lda
        complex b
        integer ldb
        complex beta
        complex c
        integer ldc
        ret = cgemm(
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine cgemvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external cgemv
        integer cgemv
        integer ret
        character trans
        integer m
        integer n
        complex alpha
        complex a
        integer lda
        complex x(n)
        integer incx
        complex beta
        complex y(n)
        integer incy
        ret = cgemv(
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine cgercwrp(
     +    ret,
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external cgerc
        integer cgerc
        integer ret
        integer m
        integer n
        complex alpha
        complex x(n)
        integer incx
        complex y(n)
        integer incy
        complex a
        integer lda
        ret = cgerc(
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine cgeruwrp(
     +    ret,
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external cgeru
        integer cgeru
        integer ret
        integer m
        integer n
        complex alpha
        complex x(n)
        integer incx
        complex y(n)
        integer incy
        complex a
        integer lda
        ret = cgeru(
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine chbmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external chbmv
        integer chbmv
        integer ret
        character uplo
        integer n
        integer k
        complex alpha
        complex a
        integer lda
        complex x(n)
        integer incx
        complex beta
        complex y(n)
        integer incy
        ret = chbmv(
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine chemmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external chemm
        integer chemm
        integer ret
        character side
        character uplo
        integer m
        integer n
        complex alpha
        complex a
        integer lda
        complex b
        integer ldb
        complex beta
        complex c
        integer ldc
        ret = chemm(
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine chemvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external chemv
        integer chemv
        integer ret
        character uplo
        integer n
        complex alpha
        complex a
        integer lda
        complex x(n)
        integer incx
        complex beta
        complex y(n)
        integer incy
        ret = chemv(
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine cherwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
        external cher
        integer cher
        integer ret
        character uplo
        integer n
        real alpha
        complex x(n)
        integer incx
        complex a
        integer lda
        ret = cher(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
      end

      subroutine cher2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external cher2
        integer cher2
        integer ret
        character uplo
        integer n
        complex alpha
        complex x(n)
        integer incx
        complex y(n)
        integer incy
        complex a
        integer lda
        ret = cher2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine cher2kwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external cher2k
        integer cher2k
        integer ret
        character uplo
        character trans
        integer n
        integer k
        complex alpha
        complex a
        integer lda
        complex b
        integer ldb
        real beta
        complex c
        integer ldc
        ret = cher2k(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine cherkwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
        external cherk
        integer cherk
        integer ret
        character uplo
        character trans
        integer n
        integer k
        real alpha
        complex a
        integer lda
        real beta
        complex c
        integer ldc
        ret = cherk(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine chpmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external chpmv
        integer chpmv
        integer ret
        character uplo
        integer n
        complex alpha
        complex ap
        complex x(n)
        integer incx
        complex beta
        complex y(n)
        integer incy
        ret = chpmv(
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine chprwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
        external chpr
        integer chpr
        integer ret
        character uplo
        integer n
        real alpha
        complex x(n)
        integer incx
        complex ap
        ret = chpr(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
      end

      subroutine chpr2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
        external chpr2
        integer chpr2
        integer ret
        character uplo
        integer n
        complex alpha
        complex x(n)
        integer incx
        complex y(n)
        integer incy
        complex ap
        ret = chpr2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
      end

      subroutine crotgwrp(
     +    ret,
     +    ca,
     +    cb,
     +    c,
     +    s
     +    )
        external crotg
        integer crotg
        integer ret
        complex ca
        complex cb
        real c
        complex s
        ret = crotg(
     +    ca,
     +    cb,
     +    c,
     +    s
     +    )
      end

      subroutine cscalwrp(
     +    ret,
     +    n,
     +    ca,
     +    cx,
     +    incx
     +    )
        external cscal
        integer cscal
        integer ret
        integer n
        complex ca
        complex cx(n)
        integer incx
        ret = cscal(
     +    n,
     +    ca,
     +    cx,
     +    incx
     +    )
      end

      subroutine csrotwrp(
     +    ret,
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy,
     +    c,
     +    s
     +    )
        external csrot
        integer csrot
        integer ret
        integer n
        complex cx(n)
        integer incx
        complex cy(n)
        integer incy
        real c
        real s
        ret = csrot(
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy,
     +    c,
     +    s
     +    )
      end

      subroutine csscalwrp(
     +    ret,
     +    n,
     +    sa,
     +    cx,
     +    incx
     +    )
        external csscal
        integer csscal
        integer ret
        integer n
        real sa
        complex cx(n)
        integer incx
        ret = csscal(
     +    n,
     +    sa,
     +    cx,
     +    incx
     +    )
      end

      subroutine cswapwrp(
     +    ret,
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
        external cswap
        integer cswap
        integer ret
        integer n
        complex cx(n)
        integer incx
        complex cy(n)
        integer incy
        ret = cswap(
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy
     +    )
      end

      subroutine csymmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external csymm
        integer csymm
        integer ret
        character side
        character uplo
        integer m
        integer n
        complex alpha
        complex a
        integer lda
        complex b
        integer ldb
        complex beta
        complex c
        integer ldc
        ret = csymm(
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine csyr2kwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external csyr2k
        integer csyr2k
        integer ret
        character uplo
        character trans
        integer n
        integer k
        complex alpha
        complex a
        integer lda
        complex b
        integer ldb
        complex beta
        complex c
        integer ldc
        ret = csyr2k(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine csyrkwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
        external csyrk
        integer csyrk
        integer ret
        character uplo
        character trans
        integer n
        integer k
        complex alpha
        complex a
        integer lda
        complex beta
        complex c
        integer ldc
        ret = csyrk(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine ctbmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ctbmv
        integer ctbmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        complex a
        integer lda
        complex x(n)
        integer incx
        ret = ctbmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine ctbsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ctbsv
        integer ctbsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        complex a
        integer lda
        complex x(n)
        integer incx
        ret = ctbsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine ctpmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external ctpmv
        integer ctpmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex ap
        complex x(n)
        integer incx
        ret = ctpmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine ctpsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external ctpsv
        integer ctpsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex ap
        complex x(n)
        integer incx
        ret = ctpsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine ctrmmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external ctrmm
        integer ctrmm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        complex alpha
        complex a
        integer lda
        complex b
        integer ldb
        ret = ctrmm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine ctrmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ctrmv
        integer ctrmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex a
        integer lda
        complex x(n)
        integer incx
        ret = ctrmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine ctrsmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external ctrsm
        integer ctrsm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        complex alpha
        complex a
        integer lda
        complex b
        integer ldb
        ret = ctrsm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine ctrsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ctrsv
        integer ctrsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex a
        integer lda
        complex x(n)
        integer incx
        ret = ctrsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine dasumwrp(
     +    ret,
     +    n,
     +    dx,
     +    incx
     +    )
        external dasum
        double precision dasum
        double precision ret
        integer n
        double precision dx(n)
        integer incx
        ret = dasum(
     +    n,
     +    dx,
     +    incx
     +    )
      end

      subroutine daxpywrp(
     +    ret,
     +    n,
     +    da,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
        external daxpy
        integer daxpy
        integer ret
        integer n
        double precision da
        double precision dx(n)
        integer incx
        double precision dy(n)
        integer incy
        ret = daxpy(
     +    n,
     +    da,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
      end

      subroutine dcabs1wrp(
     +    ret,
     +    z
     +    )
        external dcabs1
        double precision dcabs1
        double precision ret
        complex*16 z
        ret = dcabs1(
     +    z
     +    )
      end

      subroutine dcopywrp(
     +    ret,
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
        external dcopy
        integer dcopy
        integer ret
        integer n
        double precision dx(n)
        integer incx
        double precision dy(n)
        integer incy
        ret = dcopy(
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
      end

      subroutine ddotwrp(
     +    ret,
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
        external ddot
        double precision ddot
        double precision ret
        integer n
        double precision dx(n)
        integer incx
        double precision dy(n)
        integer incy
        ret = ddot(
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
      end

      subroutine dgbmvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external dgbmv
        integer dgbmv
        integer ret
        character trans
        integer m
        integer n
        integer kl
        integer ku
        double precision alpha
        double precision a
        integer lda
        double precision x(n)
        integer incx
        double precision beta
        double precision y(n)
        integer incy
        ret = dgbmv(
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine dgemmwrp(
     +    ret,
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external dgemm
        integer dgemm
        integer ret
        character transa
        character transb
        integer m
        integer n
        integer k
        double precision alpha
        double precision a
        integer lda
        double precision b
        integer ldb
        double precision beta
        double precision c
        integer ldc
        ret = dgemm(
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine dgemvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external dgemv
        integer dgemv
        integer ret
        character trans
        integer m
        integer n
        double precision alpha
        double precision a
        integer lda
        double precision x(n)
        integer incx
        double precision beta
        double precision y(n)
        integer incy
        ret = dgemv(
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine dgerwrp(
     +    ret,
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external dger
        integer dger
        integer ret
        integer m
        integer n
        double precision alpha
        double precision x(n)
        integer incx
        double precision y(n)
        integer incy
        double precision a
        integer lda
        ret = dger(
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine dnrm2wrp(
     +    ret,
     +    n,
     +    x,
     +    incx
     +    )
        external dnrm2
        double precision dnrm2
        double precision ret
        integer n
        double precision x(n)
        integer incx
        ret = dnrm2(
     +    n,
     +    x,
     +    incx
     +    )
      end

      subroutine drotwrp(
     +    ret,
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy,
     +    c,
     +    s
     +    )
        external drot
        integer drot
        integer ret
        integer n
        double precision dx(n)
        integer incx
        double precision dy(n)
        integer incy
        double precision c
        double precision s
        ret = drot(
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy,
     +    c,
     +    s
     +    )
      end

      subroutine drotgwrp(
     +    ret,
     +    da,
     +    db,
     +    c,
     +    s
     +    )
        external drotg
        integer drotg
        integer ret
        double precision da
        double precision db
        double precision c
        double precision s
        ret = drotg(
     +    da,
     +    db,
     +    c,
     +    s
     +    )
      end

      subroutine drotmwrp(
     +    ret,
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy,
     +    dparam
     +    )
        external drotm
        integer drotm
        integer ret
        integer n
        double precision dx(n)
        integer incx
        double precision dy(n)
        integer incy
        double precision dparam
        ret = drotm(
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy,
     +    dparam
     +    )
      end

      subroutine drotmgwrp(
     +    ret,
     +    dd1,
     +    dd2,
     +    dx1,
     +    dy1,
     +    dparam
     +    )
        external drotmg
        integer drotmg
        integer ret
        double precision dd1
        double precision dd2
        double precision dx1(n)
        double precision dy1(n)
        double precision dparam
        ret = drotmg(
     +    dd1,
     +    dd2,
     +    dx1,
     +    dy1,
     +    dparam
     +    )
      end

      subroutine dsbmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external dsbmv
        integer dsbmv
        integer ret
        character uplo
        integer n
        integer k
        double precision alpha
        double precision a
        integer lda
        double precision x(n)
        integer incx
        double precision beta
        double precision y(n)
        integer incy
        ret = dsbmv(
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine dscalwrp(
     +    ret,
     +    n,
     +    da,
     +    dx,
     +    incx
     +    )
        external dscal
        integer dscal
        integer ret
        integer n
        double precision da
        double precision dx(n)
        integer incx
        ret = dscal(
     +    n,
     +    da,
     +    dx,
     +    incx
     +    )
      end

      subroutine dsdotwrp(
     +    ret,
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
        external dsdot
        double precision dsdot
        double precision ret
        integer n
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        ret = dsdot(
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
      end

      subroutine dspmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external dspmv
        integer dspmv
        integer ret
        character uplo
        integer n
        double precision alpha
        double precision ap
        double precision x(n)
        integer incx
        double precision beta
        double precision y(n)
        integer incy
        ret = dspmv(
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine dsprwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
        external dspr
        integer dspr
        integer ret
        character uplo
        integer n
        double precision alpha
        double precision x(n)
        integer incx
        double precision ap
        ret = dspr(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
      end

      subroutine dspr2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
        external dspr2
        integer dspr2
        integer ret
        character uplo
        integer n
        double precision alpha
        double precision x(n)
        integer incx
        double precision y(n)
        integer incy
        double precision ap
        ret = dspr2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
      end

      subroutine dswapwrp(
     +    ret,
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
        external dswap
        integer dswap
        integer ret
        integer n
        double precision dx(n)
        integer incx
        double precision dy(n)
        integer incy
        ret = dswap(
     +    n,
     +    dx,
     +    incx,
     +    dy,
     +    incy
     +    )
      end

      subroutine dsymmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external dsymm
        integer dsymm
        integer ret
        character side
        character uplo
        integer m
        integer n
        double precision alpha
        double precision a
        integer lda
        double precision b
        integer ldb
        double precision beta
        double precision c
        integer ldc
        ret = dsymm(
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine dsymvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external dsymv
        integer dsymv
        integer ret
        character uplo
        integer n
        double precision alpha
        double precision a
        integer lda
        double precision x(n)
        integer incx
        double precision beta
        double precision y(n)
        integer incy
        ret = dsymv(
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine dsyrwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
        external dsyr
        integer dsyr
        integer ret
        character uplo
        integer n
        double precision alpha
        double precision x(n)
        integer incx
        double precision a
        integer lda
        ret = dsyr(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
      end

      subroutine dsyr2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external dsyr2
        integer dsyr2
        integer ret
        character uplo
        integer n
        double precision alpha
        double precision x(n)
        integer incx
        double precision y(n)
        integer incy
        double precision a
        integer lda
        ret = dsyr2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine dsyr2kwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external dsyr2k
        integer dsyr2k
        integer ret
        character uplo
        character trans
        integer n
        integer k
        double precision alpha
        double precision a
        integer lda
        double precision b
        integer ldb
        double precision beta
        double precision c
        integer ldc
        ret = dsyr2k(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine dsyrkwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
        external dsyrk
        integer dsyrk
        integer ret
        character uplo
        character trans
        integer n
        integer k
        double precision alpha
        double precision a
        integer lda
        double precision beta
        double precision c
        integer ldc
        ret = dsyrk(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine dtbmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external dtbmv
        integer dtbmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        double precision a
        integer lda
        double precision x(n)
        integer incx
        ret = dtbmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine dtbsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external dtbsv
        integer dtbsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        double precision a
        integer lda
        double precision x(n)
        integer incx
        ret = dtbsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine dtpmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external dtpmv
        integer dtpmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        double precision ap
        double precision x(n)
        integer incx
        ret = dtpmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine dtpsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external dtpsv
        integer dtpsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        double precision ap
        double precision x(n)
        integer incx
        ret = dtpsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine dtrmmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external dtrmm
        integer dtrmm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        double precision alpha
        double precision a
        integer lda
        double precision b
        integer ldb
        ret = dtrmm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine dtrmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external dtrmv
        integer dtrmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        double precision a
        integer lda
        double precision x(n)
        integer incx
        ret = dtrmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine dtrsmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external dtrsm
        integer dtrsm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        double precision alpha
        double precision a
        integer lda
        double precision b
        integer ldb
        ret = dtrsm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine dtrsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external dtrsv
        integer dtrsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        double precision a
        integer lda
        double precision x(n)
        integer incx
        ret = dtrsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine dzasumwrp(
     +    ret,
     +    n,
     +    zx,
     +    incx
     +    )
        external dzasum
        double precision dzasum
        double precision ret
        integer n
        complex*16 zx(n)
        integer incx
        ret = dzasum(
     +    n,
     +    zx,
     +    incx
     +    )
      end

      subroutine dznrm2wrp(
     +    ret,
     +    n,
     +    x,
     +    incx
     +    )
        external dznrm2
        double precision dznrm2
        double precision ret
        integer n
        complex*16 x(n)
        integer incx
        ret = dznrm2(
     +    n,
     +    x,
     +    incx
     +    )
      end

      subroutine icamaxwrp(
     +    ret,
     +    n,
     +    cx,
     +    incx
     +    )
        external icamax
        integer icamax
        integer ret
        integer n
        complex cx(n)
        integer incx
        ret = icamax(
     +    n,
     +    cx,
     +    incx
     +    )
      end

      subroutine idamaxwrp(
     +    ret,
     +    n,
     +    dx,
     +    incx
     +    )
        external idamax
        integer idamax
        integer ret
        integer n
        double precision dx(n)
        integer incx
        ret = idamax(
     +    n,
     +    dx,
     +    incx
     +    )
      end

      subroutine isamaxwrp(
     +    ret,
     +    n,
     +    sx,
     +    incx
     +    )
        external isamax
        integer isamax
        integer ret
        integer n
        real sx(n)
        integer incx
        ret = isamax(
     +    n,
     +    sx,
     +    incx
     +    )
      end

      subroutine izamaxwrp(
     +    ret,
     +    n,
     +    zx,
     +    incx
     +    )
        external izamax
        integer izamax
        integer ret
        integer n
        complex*16 zx(n)
        integer incx
        ret = izamax(
     +    n,
     +    zx,
     +    incx
     +    )
      end

      subroutine lsamewrp(
     +    ret,
     +    ca,
     +    cb
     +    )
        external lsame
        logical lsame
        logical ret
        character ca
        character cb
        ret = lsame(
     +    ca,
     +    cb
     +    )
      end

      subroutine sasumwrp(
     +    ret,
     +    n,
     +    sx,
     +    incx
     +    )
        external sasum
        real sasum
        real ret
        integer n
        real sx(n)
        integer incx
        ret = sasum(
     +    n,
     +    sx,
     +    incx
     +    )
      end

      subroutine saxpywrp(
     +    ret,
     +    n,
     +    sa,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
        external saxpy
        integer saxpy
        integer ret
        integer n
        real sa
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        ret = saxpy(
     +    n,
     +    sa,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
      end

      subroutine scasumwrp(
     +    ret,
     +    n,
     +    cx,
     +    incx
     +    )
        external scasum
        real scasum
        real ret
        integer n
        complex cx(n)
        integer incx
        ret = scasum(
     +    n,
     +    cx,
     +    incx
     +    )
      end

      subroutine scnrm2wrp(
     +    ret,
     +    n,
     +    x,
     +    incx
     +    )
        external scnrm2
        real scnrm2
        real ret
        integer n
        complex x(n)
        integer incx
        ret = scnrm2(
     +    n,
     +    x,
     +    incx
     +    )
      end

      subroutine scopywrp(
     +    ret,
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
        external scopy
        integer scopy
        integer ret
        integer n
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        ret = scopy(
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
      end

      subroutine sdotwrp(
     +    ret,
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
        external sdot
        real sdot
        real ret
        integer n
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        ret = sdot(
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
      end

      subroutine sdsdotwrp(
     +    ret,
     +    n,
     +    sb,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
        external sdsdot
        real sdsdot
        real ret
        integer n
        real sb
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        ret = sdsdot(
     +    n,
     +    sb,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
      end

      subroutine sgbmvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external sgbmv
        integer sgbmv
        integer ret
        character trans
        integer m
        integer n
        integer kl
        integer ku
        real alpha
        real a
        integer lda
        real x(n)
        integer incx
        real beta
        real y(n)
        integer incy
        ret = sgbmv(
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine sgemmwrp(
     +    ret,
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external sgemm
        integer sgemm
        integer ret
        character transa
        character transb
        integer m
        integer n
        integer k
        real alpha
        real a
        integer lda
        real b
        integer ldb
        real beta
        real c
        integer ldc
        ret = sgemm(
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine sgemvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external sgemv
        integer sgemv
        integer ret
        character trans
        integer m
        integer n
        real alpha
        real a
        integer lda
        real x(n)
        integer incx
        real beta
        real y(n)
        integer incy
        ret = sgemv(
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine sgerwrp(
     +    ret,
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external sger
        integer sger
        integer ret
        integer m
        integer n
        real alpha
        real x(n)
        integer incx
        real y(n)
        integer incy
        real a
        integer lda
        ret = sger(
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine snrm2wrp(
     +    ret,
     +    n,
     +    x,
     +    incx
     +    )
        external snrm2
        real snrm2
        real ret
        integer n
        real x(n)
        integer incx
        ret = snrm2(
     +    n,
     +    x,
     +    incx
     +    )
      end

      subroutine srotwrp(
     +    ret,
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy,
     +    c,
     +    s
     +    )
        external srot
        integer srot
        integer ret
        integer n
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        real c
        real s
        ret = srot(
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy,
     +    c,
     +    s
     +    )
      end

      subroutine srotgwrp(
     +    ret,
     +    sa,
     +    sb,
     +    c,
     +    s
     +    )
        external srotg
        integer srotg
        integer ret
        real sa
        real sb
        real c
        real s
        ret = srotg(
     +    sa,
     +    sb,
     +    c,
     +    s
     +    )
      end

      subroutine srotmwrp(
     +    ret,
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy,
     +    sparam
     +    )
        external srotm
        integer srotm
        integer ret
        integer n
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        real sparam
        ret = srotm(
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy,
     +    sparam
     +    )
      end

      subroutine srotmgwrp(
     +    ret,
     +    sd1,
     +    sd2,
     +    sx1,
     +    sy1,
     +    sparam
     +    )
        external srotmg
        integer srotmg
        integer ret
        real sd1
        real sd2
        real sx1(n)
        real sy1(n)
        real sparam
        ret = srotmg(
     +    sd1,
     +    sd2,
     +    sx1,
     +    sy1,
     +    sparam
     +    )
      end

      subroutine ssbmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external ssbmv
        integer ssbmv
        integer ret
        character uplo
        integer n
        integer k
        real alpha
        real a
        integer lda
        real x(n)
        integer incx
        real beta
        real y(n)
        integer incy
        ret = ssbmv(
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine sscalwrp(
     +    ret,
     +    n,
     +    sa,
     +    sx,
     +    incx
     +    )
        external sscal
        integer sscal
        integer ret
        integer n
        real sa
        real sx(n)
        integer incx
        ret = sscal(
     +    n,
     +    sa,
     +    sx,
     +    incx
     +    )
      end

      subroutine sspmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external sspmv
        integer sspmv
        integer ret
        character uplo
        integer n
        real alpha
        real ap
        real x(n)
        integer incx
        real beta
        real y(n)
        integer incy
        ret = sspmv(
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine ssprwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
        external sspr
        integer sspr
        integer ret
        character uplo
        integer n
        real alpha
        real x(n)
        integer incx
        real ap
        ret = sspr(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
      end

      subroutine sspr2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
        external sspr2
        integer sspr2
        integer ret
        character uplo
        integer n
        real alpha
        real x(n)
        integer incx
        real y(n)
        integer incy
        real ap
        ret = sspr2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
      end

      subroutine sswapwrp(
     +    ret,
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
        external sswap
        integer sswap
        integer ret
        integer n
        real sx(n)
        integer incx
        real sy(n)
        integer incy
        ret = sswap(
     +    n,
     +    sx,
     +    incx,
     +    sy,
     +    incy
     +    )
      end

      subroutine ssymmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external ssymm
        integer ssymm
        integer ret
        character side
        character uplo
        integer m
        integer n
        real alpha
        real a
        integer lda
        real b
        integer ldb
        real beta
        real c
        integer ldc
        ret = ssymm(
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine ssymvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external ssymv
        integer ssymv
        integer ret
        character uplo
        integer n
        real alpha
        real a
        integer lda
        real x(n)
        integer incx
        real beta
        real y(n)
        integer incy
        ret = ssymv(
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine ssyrwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
        external ssyr
        integer ssyr
        integer ret
        character uplo
        integer n
        real alpha
        real x(n)
        integer incx
        real a
        integer lda
        ret = ssyr(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
      end

      subroutine ssyr2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external ssyr2
        integer ssyr2
        integer ret
        character uplo
        integer n
        real alpha
        real x(n)
        integer incx
        real y(n)
        integer incy
        real a
        integer lda
        ret = ssyr2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine ssyr2kwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external ssyr2k
        integer ssyr2k
        integer ret
        character uplo
        character trans
        integer n
        integer k
        real alpha
        real a
        integer lda
        real b
        integer ldb
        real beta
        real c
        integer ldc
        ret = ssyr2k(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine ssyrkwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
        external ssyrk
        integer ssyrk
        integer ret
        character uplo
        character trans
        integer n
        integer k
        real alpha
        real a
        integer lda
        real beta
        real c
        integer ldc
        ret = ssyrk(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine stbmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external stbmv
        integer stbmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        real a
        integer lda
        real x(n)
        integer incx
        ret = stbmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine stbsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external stbsv
        integer stbsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        real a
        integer lda
        real x(n)
        integer incx
        ret = stbsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine stpmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external stpmv
        integer stpmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        real ap
        real x(n)
        integer incx
        ret = stpmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine stpsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external stpsv
        integer stpsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        real ap
        real x(n)
        integer incx
        ret = stpsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine strmmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external strmm
        integer strmm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        real alpha
        real a
        integer lda
        real b
        integer ldb
        ret = strmm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine strmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external strmv
        integer strmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        real a
        integer lda
        real x(n)
        integer incx
        ret = strmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine strsmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external strsm
        integer strsm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        real alpha
        real a
        integer lda
        real b
        integer ldb
        ret = strsm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine strsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external strsv
        integer strsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        real a
        integer lda
        real x(n)
        integer incx
        ret = strsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine zaxpywrp(
     +    ret,
     +    n,
     +    za,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
        external zaxpy
        integer zaxpy
        integer ret
        integer n
        complex*16 za
        complex*16 zx(n)
        integer incx
        complex*16 zy(n)
        integer incy
        ret = zaxpy(
     +    n,
     +    za,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
      end

      subroutine zcopywrp(
     +    ret,
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
        external zcopy
        integer zcopy
        integer ret
        integer n
        complex*16 zx(n)
        integer incx
        complex*16 zy(n)
        integer incy
        ret = zcopy(
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
      end

      subroutine zdotcwrp(
     +    ret,
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
        external wzdotc
        complex*16 wzdotc
        complex*16 ret
        integer n
        complex*16 zx(n)
        integer incx
        complex*16 zy(n)
        integer incy
        ret = wzdotc(
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
      end

      subroutine zdotuwrp(
     +    ret,
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
        external wzdotu
        complex*16 wzdotu
        complex*16 ret
        integer n
        complex*16 zx(n)
        integer incx
        complex*16 zy(n)
        integer incy
        ret = wzdotu(
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
      end

      subroutine zdrotwrp(
     +    ret,
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy,
     +    c,
     +    s
     +    )
        external zdrot
        integer zdrot
        integer ret
        integer n
        complex*16 cx(n)
        integer incx
        complex*16 cy(n)
        integer incy
        double precision c
        double precision s
        ret = zdrot(
     +    n,
     +    cx,
     +    incx,
     +    cy,
     +    incy,
     +    c,
     +    s
     +    )
      end

      subroutine zdscalwrp(
     +    ret,
     +    n,
     +    da,
     +    zx,
     +    incx
     +    )
        external zdscal
        integer zdscal
        integer ret
        integer n
        double precision da
        complex*16 zx(n)
        integer incx
        ret = zdscal(
     +    n,
     +    da,
     +    zx,
     +    incx
     +    )
      end

      subroutine zgbmvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external zgbmv
        integer zgbmv
        integer ret
        character trans
        integer m
        integer n
        integer kl
        integer ku
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        complex*16 beta
        complex*16 y(n)
        integer incy
        ret = zgbmv(
     +    trans,
     +    m,
     +    n,
     +    kl,
     +    ku,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine zgemmwrp(
     +    ret,
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external zgemm
        integer zgemm
        integer ret
        character transa
        character transb
        integer m
        integer n
        integer k
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 b
        integer ldb
        complex*16 beta
        complex*16 c
        integer ldc
        ret = zgemm(
     +    transa,
     +    transb,
     +    m,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine zgemvwrp(
     +    ret,
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external zgemv
        integer zgemv
        integer ret
        character trans
        integer m
        integer n
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        complex*16 beta
        complex*16 y(n)
        integer incy
        ret = zgemv(
     +    trans,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine zgercwrp(
     +    ret,
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external zgerc
        integer zgerc
        integer ret
        integer m
        integer n
        complex*16 alpha
        complex*16 x(n)
        integer incx
        complex*16 y(n)
        integer incy
        complex*16 a
        integer lda
        ret = zgerc(
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine zgeruwrp(
     +    ret,
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external zgeru
        integer zgeru
        integer ret
        integer m
        integer n
        complex*16 alpha
        complex*16 x(n)
        integer incx
        complex*16 y(n)
        integer incy
        complex*16 a
        integer lda
        ret = zgeru(
     +    m,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine zhbmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external zhbmv
        integer zhbmv
        integer ret
        character uplo
        integer n
        integer k
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        complex*16 beta
        complex*16 y(n)
        integer incy
        ret = zhbmv(
     +    uplo,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine zhemmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external zhemm
        integer zhemm
        integer ret
        character side
        character uplo
        integer m
        integer n
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 b
        integer ldb
        complex*16 beta
        complex*16 c
        integer ldc
        ret = zhemm(
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine zhemvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external zhemv
        integer zhemv
        integer ret
        character uplo
        integer n
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        complex*16 beta
        complex*16 y(n)
        integer incy
        ret = zhemv(
     +    uplo,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine zherwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
        external zher
        integer zher
        integer ret
        character uplo
        integer n
        double precision alpha
        complex*16 x(n)
        integer incx
        complex*16 a
        integer lda
        ret = zher(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    a,
     +    lda
     +    )
      end

      subroutine zher2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
        external zher2
        integer zher2
        integer ret
        character uplo
        integer n
        complex*16 alpha
        complex*16 x(n)
        integer incx
        complex*16 y(n)
        integer incy
        complex*16 a
        integer lda
        ret = zher2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    a,
     +    lda
     +    )
      end

      subroutine zher2kwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external zher2k
        integer zher2k
        integer ret
        character uplo
        character trans
        integer n
        integer k
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 b
        integer ldb
        double precision beta
        complex*16 c
        integer ldc
        ret = zher2k(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine zherkwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
        external zherk
        integer zherk
        integer ret
        character uplo
        character trans
        integer n
        integer k
        double precision alpha
        complex*16 a
        integer lda
        double precision beta
        complex*16 c
        integer ldc
        ret = zherk(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine zhpmvwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
        external zhpmv
        integer zhpmv
        integer ret
        character uplo
        integer n
        complex*16 alpha
        complex*16 ap
        complex*16 x(n)
        integer incx
        complex*16 beta
        complex*16 y(n)
        integer incy
        ret = zhpmv(
     +    uplo,
     +    n,
     +    alpha,
     +    ap,
     +    x,
     +    incx,
     +    beta,
     +    y,
     +    incy
     +    )
      end

      subroutine zhprwrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
        external zhpr
        integer zhpr
        integer ret
        character uplo
        integer n
        double precision alpha
        complex*16 x(n)
        integer incx
        complex*16 ap
        ret = zhpr(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    ap
     +    )
      end

      subroutine zhpr2wrp(
     +    ret,
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
        external zhpr2
        integer zhpr2
        integer ret
        character uplo
        integer n
        complex*16 alpha
        complex*16 x(n)
        integer incx
        complex*16 y(n)
        integer incy
        complex*16 ap
        ret = zhpr2(
     +    uplo,
     +    n,
     +    alpha,
     +    x,
     +    incx,
     +    y,
     +    incy,
     +    ap
     +    )
      end

      subroutine zrotgwrp(
     +    ret,
     +    ca,
     +    cb,
     +    c,
     +    s
     +    )
        external zrotg
        integer zrotg
        integer ret
        complex*16 ca
        complex*16 cb
        double precision c
        complex*16 s
        ret = zrotg(
     +    ca,
     +    cb,
     +    c,
     +    s
     +    )
      end

      subroutine zscalwrp(
     +    ret,
     +    n,
     +    za,
     +    zx,
     +    incx
     +    )
        external zscal
        integer zscal
        integer ret
        integer n
        complex*16 za
        complex*16 zx(n)
        integer incx
        ret = zscal(
     +    n,
     +    za,
     +    zx,
     +    incx
     +    )
      end

      subroutine zswapwrp(
     +    ret,
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
        external zswap
        integer zswap
        integer ret
        integer n
        complex*16 zx(n)
        integer incx
        complex*16 zy(n)
        integer incy
        ret = zswap(
     +    n,
     +    zx,
     +    incx,
     +    zy,
     +    incy
     +    )
      end

      subroutine zsymmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external zsymm
        integer zsymm
        integer ret
        character side
        character uplo
        integer m
        integer n
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 b
        integer ldb
        complex*16 beta
        complex*16 c
        integer ldc
        ret = zsymm(
     +    side,
     +    uplo,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine zsyr2kwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
        external zsyr2k
        integer zsyr2k
        integer ret
        character uplo
        character trans
        integer n
        integer k
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 b
        integer ldb
        complex*16 beta
        complex*16 c
        integer ldc
        ret = zsyr2k(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine zsyrkwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
        external zsyrk
        integer zsyrk
        integer ret
        character uplo
        character trans
        integer n
        integer k
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 beta
        complex*16 c
        integer ldc
        ret = zsyrk(
     +    uplo,
     +    trans,
     +    n,
     +    k,
     +    alpha,
     +    a,
     +    lda,
     +    beta,
     +    c,
     +    ldc
     +    )
      end

      subroutine ztbmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ztbmv
        integer ztbmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        ret = ztbmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine ztbsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ztbsv
        integer ztbsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        integer k
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        ret = ztbsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    k,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine ztpmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external ztpmv
        integer ztpmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex*16 ap
        complex*16 x(n)
        integer incx
        ret = ztpmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine ztpsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
        external ztpsv
        integer ztpsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex*16 ap
        complex*16 x(n)
        integer incx
        ret = ztpsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    ap,
     +    x,
     +    incx
     +    )
      end

      subroutine ztrmmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external ztrmm
        integer ztrmm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 b
        integer ldb
        ret = ztrmm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine ztrmvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ztrmv
        integer ztrmv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        ret = ztrmv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end

      subroutine ztrsmwrp(
     +    ret,
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
        external ztrsm
        integer ztrsm
        integer ret
        character side
        character uplo
        character transa
        character diag
        integer m
        integer n
        complex*16 alpha
        complex*16 a
        integer lda
        complex*16 b
        integer ldb
        ret = ztrsm(
     +    side,
     +    uplo,
     +    transa,
     +    diag,
     +    m,
     +    n,
     +    alpha,
     +    a,
     +    lda,
     +    b,
     +    ldb
     +    )
      end

      subroutine ztrsvwrp(
     +    ret,
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
        external ztrsv
        integer ztrsv
        integer ret
        character uplo
        character trans
        character diag
        integer n
        complex*16 a
        integer lda
        complex*16 x(n)
        integer incx
        ret = ztrsv(
     +    uplo,
     +    trans,
     +    diag,
     +    n,
     +    a,
     +    lda,
     +    x,
     +    incx
     +    )
      end
